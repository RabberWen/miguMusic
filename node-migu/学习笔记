1、项目当中思路。
2、技术点。项目。
***************************************************************
react打包：cnpm run build
node 上线。
1、路由刷新问题。history
    1、下载模块
        cnpm install connect-history-api-fallback -S
    2、引入
2、代理的问题。
    1、下载模块
        cnpm install http-proxy-middleware -s
    2、引入
        const {createProxyMiddleware} = require("http-proxy-middleware");
**********************